{
  "name": "timestamp-microservice",
  "version": "1.0.0",
  "description": "I can pass a string as a parameter, and it will check to see whether that string contains either a unix timestamp or a natural language date. If it does, it returns both the Unix timestamp and the natural language form of that date.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "author": "vinaypuppal",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.15.1",
    "express": "^4.13.4",
    "moment": "^2.13.0",
    "morgan": "^1.7.0",
    "nodemon": "^1.9.2"
  }
}
